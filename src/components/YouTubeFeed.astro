<!-- ---
import { onMount } from "astro/client";

let videos = [];

onMount(async () => {
  try {
    const response = await fetch(
      "https://www.googleapis.com/youtube/v3/search?key=YOUR_API_KEY&channelId=UCXXXXXXXXXXXX&part=snippet,id&order=date&maxResults=5"
    );
    const data = await response.json();
    videos = data.items;
  } catch (error) {
    console.error("Error fetching YouTube videos:", error);
  }
});
---

<section class="py-16 px-6 bg-gray-100 dark:bg-gray-800">
  <h2 class="text-3xl font-semibold text-center mb-8">Latest Videos</h2>
  <div class="grid gap-6 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
    {
      videos.map((video) => (
        <div class="bg-white dark:bg-gray-900 rounded-lg shadow-md overflow-hidden">
          <a
            href={`https://www.youtube.com/watch?v=${video.id.videoId}`}
            target="_blank"
            rel="noopener noreferrer"
          >
            <img
              src={video.snippet.thumbnails.high.url}
              alt={video.snippet.title}
              class="w-full h-48 object-cover"
            />
            <div class="p-4">
              <h3 class="text-lg font-semibold text-gray-800 dark:text-white">
                {video.snippet.title}
              </h3>
              <p class="text-sm text-gray-600 dark:text-gray-400">
                {new Date(video.snippet.publishedAt).toLocaleDateString()}
              </p>
            </div>
          </a>
        </div>
      ))
    }
  </div>
</section> -->
